# Local dev symlink to the upstream ruvector source tree. Only the
# pre-built WASM artifacts under vendor/ruvector/ are needed at runtime.
/ruvector

# Debug/validation screenshots from agent-browser runs
docs/validation/screenshots/debug-*.png

# Node (Playwright + a11y tooling)
node_modules/
test-results/

# OS noise
.DS_Store
Thumbs.db

# Local Cloudflare runtime / deploy staging
.wrangler/
.pages-upload/
