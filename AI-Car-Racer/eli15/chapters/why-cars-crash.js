// eli15/chapters/why-cars-crash.js
// Four mechanistic reasons cars drive into walls — the "why is my car dumb?" FAQ.
export default {
  id: 'why-cars-crash',
  title: 'Why your car keeps driving into walls',
  oneLiner: 'Four reasons: frozen reflexes, random gen-0, elite lock-in, and physics.',
  body: [
    '<p>Watching gen 0 is painful. Cars spin in place, reverse into walls, or pick a',
    'direction and commit to it until something solid stops them. "Why don\'t they <em>learn</em>?"',
    'is the question everyone asks. The honest answer has four parts, and none of them are',
    '"the AI isn\'t smart enough yet."</p>',

    '<h3>1. The brain is a frozen reflex table, not an agent</h3>',
    '<p>Every car\'s "brain" is the 244-number neural net from the',
    '<code>neural-network</code> chapter. Once a car is born, those 244 numbers are <strong>locked</strong>',
    'for its entire life. No learning, no memory, no "oh I bumped that wall, I\'ll try something',
    'different next time." Each frame it does the same thing: feed sensor readings in the top,',
    'read four booleans out the bottom. It\'s a <em>lookup function</em> dressed up as a driver.',
    'Learning only happens <em>between</em> generations, when the genetic algorithm throws out',
    'losers and copies winners (with noise).</p>',

    '<h3>2. Generation 0 is a bag of dice rolls</h3>',
    '<p>When a fresh population is born, every weight and bias is a uniform random number',
    'in [-1, 1] — see <code>network.js:73</code>: <code>w[k] = Math.random()*2-1</code>.',
    'That means ~1 in 16 gen-0 brains happen to roll a bias pattern that makes the',
    '<em>forward</em> output neuron fire regardless of what the sensors say. Those cars are',
    '"always-forward zombies" — they drive perfectly straight until the first corner',
    'vaporises them. Another chunk roll "always-reverse" or "always-left" and spin in place.',
    'The rare few that happen to wire up a sensible reflex — "if the centre ray reads low,',
    'stop going forward" — are the seeds everything else descends from. Gen 0 isn\'t dumb;',
    'it\'s <strong>random</strong>, and random is mostly broken.</p>',

    '<h3>3. The first half-decent car becomes the ancestor of everyone</h3>',
    '<p>Fitness is brutally simple: <code>checkPointsCount + laps × cpLen</code>',
    '(<code>sim-worker.js:331</code>). Whichever car grabs the first checkpoint <em>first</em>',
    'wins the generation, even if it immediately crashes one metre later. The GA then copies',
    'that winner\'s 244 weights, sprinkles a bit of mutation noise, and the whole next',
    'generation is its near-siblings. After ~10 generations of this, the population has',
    '<strong>locked in</strong> on whatever reflex the first lucky car happened to have —',
    'even if that reflex is "hug the left wall at 0.3 × max speed." Escaping that local',
    'optimum takes either lots of mutation or a track change. This is why you\'ll see',
    'apparent progress plateau, then collapse when the track geometry punishes the inherited',
    'habit.</p>',

    '<h3>4. At full speed, physics beats reactivity</h3>',
    '<p>The sensors see 400 pixels ahead (<code>sensor.js:5</code>:',
    '<code>this.rayLength=400</code>). At top speed the car\'s stopping distance — decelerate,',
    'reverse, come to rest — is <strong>closer to 450 pixels</strong>. That means when a',
    'ray screams "WALL!" at max range, the car is <em>already</em> too close to stop. A',
    'pure reactive brain (no memory, no speed-based anticipation) literally cannot avoid',
    'the crash; the wall was inside the physics horizon before the sensor saw it.',
    'The NN gets speed as an input and <em>could</em> learn "when fast, brake earlier," but',
    'that\'s a compound rule — sensor × speed × output — and the 10→16→4 topology',
    '(<code>car.js:42</code>: <code>[this.sensor.rayCount+3,16,4]</code>) has to discover',
    'it from pure selection pressure. Until it does, the fastest cars are also the',
    'deadest.</p>',

    '<h3>So what fixes it?</h3>',
    '<p>Each of these has a lever the project already pulls on:</p>',
    '<ul>',
    '<li><strong>Frozen brains</strong> — not "fixed" so much as accepted. That\'s why we lean',
    'on the genetic algorithm and on seeding from past-life winners (see <code>track-similarity</code>).</li>',
    '<li><strong>Random gen 0</strong> — the conservative-init slider biases initial weights',
    'toward "go forward when clear, slow when close" so gen 0 isn\'t pure chaos.</li>',
    '<li><strong>Elite lock-in</strong> — higher mutation rate, population diversity bonuses, and',
    'retrieval from ReasoningBank (different ancestors for different situations).</li>',
    '<li><strong>Physics horizon</strong> — longer rays, a speed-dependent "virtual ray length,"',
    'or giving the NN a frame or two of sensor history so it can infer closing rate.</li>',
    '</ul>',

    '<h3>Try it yourself</h3>',
    '<ul>',
    '<li>Start a run and pause at gen 0. Count how many cars drive perfectly straight into',
    'the first wall without flinching — those are the always-forward zombies.</li>',
    '<li>Around gen 10, look at the lineage viewer (🌳). Most of the surviving population',
    'usually traces back to one or two gen-3-ish ancestors. That\'s elite lock-in, visualised.</li>',
    '<li>In phase 4, watch the speed indicator when the best car crashes. If it\'s near max,',
    'that\'s the physics-horizon problem, not a brain bug.</li>',
    '</ul>',
  ].join('\n'),
  diagram: [
    '<pre class="eli15-ascii">',
    '  Why a car crashes — decision tree',
    '',
    '  Did the brain even have a chance?',
    '   │',
    '   ├─ NO  →  gen-0 random roll                 (reason 2)',
    '   │        e.g. "forward" bias > any sensor sum',
    '   │        → car drives straight into wall',
    '   │',
    '   ├─ NO  →  inherited a bad reflex             (reason 3)',
    '   │        first lucky ancestor\'s habit cloned',
    '   │        → whole population fails same way',
    '   │',
    '   └─ YES →  brain reacted, but too late        (reason 4)',
    '             sensor range  = 400 px',
    '             stop distance ≈ 450 px',
    '             → wall inside physics horizon',
    '',
    '  And remember: the brain NEVER updates mid-life   (reason 1)',
    '  — learning only happens at the generation boundary.',
    '</pre>',
  ].join('\n'),
  related: [
    'neural-network',
    'sensors',
    'genetic-algorithm',
    'fitness-function',
  ],
};
